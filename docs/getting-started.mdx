---
title: "Getting Started"
description: "A quick guide to integrating Headless Calendar into your JavaScript applications using various frameworks."
---

# Getting Started

This guide will help you quickly integrate Headless Calendar into your project. While the core library is framework-agnostic, we'll demonstrate a basic setup using each framework we support to illustrate the workflow. Remember that detailed instructions for each framework are available in the "Frameworks" section.

## 1. Installation

First, install the wrapper for your chosen framework.

<MarkdownTabs library="calendar">
  <Tab active={true} label="React" icon={<SiReact />}>
    ```bash
    # Using npm
    npm install @verbpatch/react-calendar

    # Using yarn
    yarn add @verbpatch/react-calendar

    # Using pnpm
    pnpm add @verbpatch/react-calendar
    ```
  </Tab>
  <Tab active={false} label="Vue.js" icon={<SiVuedotjs />}>
    ```bash
    # Using npm
    npm install @verbpatch/vuejs-calendar

    # Using yarn
    yarn add @verbpatch/vuejs-calendar

    # Using pnpm
    pnpm add @verbpatch/vuejs-calendar
    ```
  </Tab>
  <Tab active={false} label="Svelte" icon={<SiSvelte />}>
    ```bash
    # Using npm
    npm install @verbpatch/svelte-calendar

    # Using yarn
    yarn add @verbpatch/svelte-calendar

    # Using pnpm
    pnpm add @verbpatch/svelte-calendar
    ```
  </Tab>
  <Tab active={false} label="Angular" icon={<SiAngular />}>
    ```bash
    # Using npm
    npm install @verbpatch/angular-calendar

    # Using yarn
    yarn add @verbpatch/angular-calendar

    # Using pnpm
    pnpm add @verbpatch/angular-calendar
    ```
  </Tab>
  <Tab active={false} label="JQuery" icon={<SiJquery />}>
    ```bash
    # Using npm
    npm install @verbpatch/jquery-calendar

    # Using yarn
    yarn add @verbpatch/jquery-calendar

    # Using pnpm
    pnpm add @verbpatch/jquery-calendar

    # or use direct link
    <script src="https://cdn.jsdelivr.net/npm/@verbpatch/jquery-calendar/dist/index.umd.js" />
    ```
  </Tab>
  <Tab active={false} label="Lit" icon={<SiLit />}>
    ```bash
    # Using npm
    npm install @verbpatch/lit-calendar

    # Using yarn
    yarn add @verbpatch/lit-calendar

    # Using pnpm
    pnpm add @verbpatch/lit-calendar
    ```
  </Tab>
  <Tab active={false} label="Preact" icon={<SiPreact />}>
    ```bash
    # Using npm
    npm install @verbpatch/preact-calendar

    # Using yarn
    yarn add @verbpatch/preact-calendar

    # Using pnpm
    pnpm add @verbpatch/preact-calendar
    ```
  </Tab>
  <Tab active={false} label="Qwik" icon={<SiQwik />}>
    ```bash
    # Using npm
    npm install @verbpatch/qwik-calendar

    # Using yarn
    yarn add @verbpatch/qwik-calendar

    # Using pnpm
    pnpm add @verbpatch/qwik-calendar
    ```
  </Tab>
  <Tab active={false} label="SolidJS" icon={<SiSolid />}>
    ```bash
    # Using npm
    npm install @verbpatch/solidjs-calendar

    # Using yarn
    yarn add @verbpatch/solidjs-calendar

    # Using pnpm
    pnpm add @verbpatch/solidjs-calendar
    ```
  </Tab>
  <Tab active={false} label="Vanilla" icon={<SiJavascript />}>
    ```bash
    # Using npm
    npm install @verbpatch/headless-calendar

    # Using yarn
    yarn add @verbpatch/headless-calendar

    # Using pnpm
    pnpm add @verbpatch/headless-calendar
    ```
  </Tab>
</MarkdownTabs>

## 2. Basic Usage

Here's a minimal example of how to set up a basic month/week/day view calendar using different framework wrappers. These examples demonstrate how the framework-specific `useCalendar` hook integrates with the core `headless-calendar` logic to provide a reactive calendar instance.

<MarkdownTabs library="calendar">
  <Tab active={true} label="React" icon={<SiReact />} examplePath="React/basic" sourcePath="src/calendar.tsx">
    &nbsp;
  </Tab>
  <Tab active={false} label="Vue.js" icon={<SiVuedotjs />} examplePath="Vuejs/basic" sourcePath="src/calendar.vue">
    &nbsp;
  </Tab>
  <Tab active={false} label="Svelte" icon={<SiSvelte />} examplePath="Svelte/basic" sourcePath="src/routes/calendar.svelte">
    &nbsp;
  </Tab>
  <Tab active={false} label="Angular" icon={<SiAngular />} examplePath="Angular/minimal" sourcePath="src/app/app.ts">
    &nbsp;
  </Tab>
  <Tab active={false} label="JQuery" icon={<SiJquery />} examplePath="jQuery" sourcePath="minimal-example.html">
    &nbsp;
  </Tab>
  <Tab active={false} label="Lit" icon={<SiLit />} examplePath="Lit/minimal" sourcePath="src/calendar-demo.ts">
    &nbsp;
  </Tab>
  <Tab active={false} label="Preact" icon={<SiPreact />} examplePath="Preact/minimal" sourcePath="src/CalendarDemo.tsx">
    &nbsp;
  </Tab>
  <Tab active={false} label="Qwik" icon={<SiQwik />} examplePath="Qwik/minimal" sourcePath="src/app.tsx">
    &nbsp;
  </Tab>
  <Tab active={false} label="SolidJS" icon={<SiSolid />} examplePath="Solidjs/minimal" sourcePath="src/CalendarDemo.tsx">
    &nbsp;
  </Tab>
  <Tab active={false} label="Vanilla" icon={<SiJavascript />} examplePath="Vanilla/minimal" sourcePath="src/calendar.js">
    &nbsp;
  </Tab>
</MarkdownTabs>

For more detailed examples and specific framework patterns, please refer to the "Frameworks" section.